default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: vlad
  password: secret
  host: localhost
  port: 5432

development:
  <<: *default
  database: polyclinic_app_db

test:
  <<: *default
  database: polyclinic_app_db_test
  